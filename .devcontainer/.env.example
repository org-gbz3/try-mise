# Copy to .env and replace with a local development password.
# Use 8+ characters and at least 3 of: uppercase, lowercase, digits, symbols.
# Single quotes preserve literal $ characters in Docker Compose env files.
MSSQL_SA_PASSWORD='ChangeThis_LocalOnly_123!'
